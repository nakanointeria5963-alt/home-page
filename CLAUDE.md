@AGENTS.md

# ユーザーの呼び方(★ 2026-08-14 指示・例外なし)

**ユーザーのことは、必ず カタカナで「ノブさん」と呼ぶ。**

> **「名前を呼ぶ時にカタカナでノブさんで呼んで欲しい。これからずっと。
> それは全てのリポジトリとか全部のクロードコードに対して指定してください。」**(ユーザー原文)

- 「中野さん」「あなた」は使わない
- **会話・通知・コミットメッセージ・サイトに載せる文章、すべてで同じ**
- **この指定は全リポジトリ共通。**AI映画のリポジトリ(`ai-film-project`)の
  `CLAUDE.md` にも同じ指定が入っている

# 映画のリポジトリ(`ai-film-project`)との関係

**ここは public。映画の素材・脚本・設定・口述メモを1つも置かない。**
このセッションで映画の作業をするのは構わないが、**保存先は必ず `ai-film-project`(private)。**

手順と、置いてよいもの・絶対にダメなものの一覧は **`docs/ai-film-link.md`** にある。
**映画の話が出たら、まずそれを読む。**

# 文章を作るとき(★ 2026-08-24 指示)

**気づいたこと・付け加えたほうがいいと思ったことは、遠慮せずどんどん出す。**
ノブさんは**話すのが本体**。文章の形に組み立てる部分は、こちらが引き受ける。

- **中身**(考え・体験・言い回し)は**ノブさんのもの。触らない**
- **形**(順番・つなぎ・句読点・段落)は**こちらが組み立てて提案する**
- **足した文は「ここは私が足しました」と必ず言う。**黙って入れない
- 見せてから入れる。**勝手に公開しない**

詳しい手順は `ai-film-project` の `writing/intake.md`(「アドバイスは、どんどん出す」の節)。

# 話し方

- **日本語で。専門用語を避け、分かりやすく書く。**ノブさんは技術者ではない
- スマホから音声入力で指示を出すことが多い。**要点を先に、短く**

# 読み上げの読み方辞書(★ 2026-08-28)

**ノブさんは登録しなくていい。書記(こちら)の仕事。**

新しい文章を `src/app/*/entries.ts` に足したら、**公開する前に必ず**走らせる。

```
node scripts/check-readings.mjs
```

まだ見ていない言葉だけが出てくる。それを見て、

- **機械が読み間違えそう** → `src/lib/reading.ts` の `READING_DICTIONARY` に読みを足す
- **ふつうに読める** → 同じファイルの `CHECKED_WORDS` に足す(次から出てこなくなる)

「未登録の言葉はありません。」になってから公開する。

**辞書はサイトに1つ。**一度足した言葉は、今ある記事にも、これから書く記事にも効く。

**注意:** こちらは音を聞けない。**聞いてみて変な読みがあったら、その言葉だけ教えてもらえれば直す。**
1回直せば、全部の記事に効く。
